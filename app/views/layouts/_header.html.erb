<header class="shadow-sm">
  <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container">
      <a class="navbar-brand p-3" href="/">NAGANO CAKE</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ms-auto">
              <% if admin_signed_in? %>
              <li class="nav-item"> <%= link_to "ログアウト（管理者用）", destroy_admin_session_path, method: :delete, class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"> <%= link_to "新規商品登録", new_admins_item_path,  class: 'nav-link text-dark list-group-item-action' %></li>
              <% else %>
              <li class="nav-item"><%= link_to "ログイン（管理者用）", new_admin_session_path, class: 'nav-link text-dark list-group-item-action' %></li>
              <% end %>
              <% if customer_signed_in? %>
              <li class="nav-item"><%= link_to 'マイページ', customer_path, class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"><%= link_to '商品一覧',items_path, class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"><%= link_to 'カート', cart_items_path, class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"><%= link_to 'ログアウト', destroy_customer_session_path, method: :delete, class: 'nav-link text-dark list-group-item-action' %></li>
              <% else %>
              <li class="nav-item"><%= link_to 'About',about_path,class: "nav-link text-dark list-group-item-action" %></li>
              <li class="nav-item"><%= link_to '商品一覧',items_path,class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"><%= link_to '新規登録', new_customer_registration_path,class: 'nav-link text-dark list-group-item-action' %></li>
              <li class="nav-item"><%= link_to 'ログイン', new_customer_session_path,class: 'nav-link text-dark list-group-item-action' %></li>
              <% end %>
              
              <% if admin_signed_in? %>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                  <%= f.text_field :content %>
                  <%= f.select :model, options_for_select({ "商品" => "item", "会員" => "customer"}) %>
                  <%= f.select :method, options_for_select({ "完全に一致" => "perfect", "部分一致" => "partical"}) %>
                  <%= f.submit "検索" %>
              <% end %>
             
              <% else %>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                  <%= f.text_field :content %>
                  <%= f.select :model, options_for_select({ "商品" => "item", "ジャンル" => "genre"}) %>
                  <%= f.select :method, options_for_select({ "完全に一致" => "perfect", "部分一致" => "partical"}) %>
                  <%= f.submit "検索" %>
                <% end %>
              <% end %>
              
          
              
              
            </ul>
          </div>
    </div>
  </nav>
</header>